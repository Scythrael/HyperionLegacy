# 0.13.6 "Item Lifecycle" QA sheet

> ⚡ **The live checklist is now [2026-09-16-0.13.6-qa.html](2026-09-16-0.13.6-qa.html)** (two columns, Claude + you, with a completion progress bar). Open that in a browser to run the pass; it is the single source of truth going forward. This Markdown file is kept as a plaintext mirror + the findings record.

---


Run on **devpreview** (staging). Do a full pass on **desktop** and again on **mobile** (or force-mobile).
Each item: do the steps, confirm the expected, and glance at the regression watch. Check the box when it passes.

Legend: **[D]** desktop-only detail, **[M]** mobile-only detail, otherwise both.

### Automated pre-check status (Claude, 2026-09-18, local internal preview)
Boxes are LEFT UNCHECKED on purpose so the user's own full pass starts clean. These tags only say what the in-client preview could and could not reach; they are not a substitute for the user's run. Tag key on individual items below:
- `✅ auto` = exercised in the local preview and behaved as written (platform noted).
- `⚠️ needs real save` = the local sandbox starts as a fresh game, so this needs the user's migrated save on devpreview.
- `⚠️ needs device` = true mobile / touch or a visual-subjective call, left to the user.
- (untagged) = not attempted in the automated pass.

**Covered this pass (2026-09-18):** the whole **Favorite/Lock** feature (§6) end to end on desktop AND forced-mobile; version string + patch-notes lede (§0); the ship-detail one-row header and BR-in-roster spot checks (§8/§9). No console errors across the whole session.
**Two findings logged at the bottom** (Armory picker silent no-op on a locked spare; vestigial fuel stats still visible). See "Notes / found issues".

---

## 0. Setup / smoke
- [ ] Header reads **v0.13.6** and the patch-notes entry shows "Item Lifecycle" with its sections rendered (not one run-on block). — `✅ auto` (version string + lede confirmed; section rendering not re-opened this pass)
- [ ] Load an EXISTING save (pre-0.13.6). It migrates without error; nothing you owned is missing. — `⚠️ needs real save`
- [ ] Any captain that was running **Local Deuterium Skim** is now **idle** (recalled), not stuck on a mission you can't see. — `⚠️ needs real save`
- [ ] Anything you had **favorited** before 0.13.6 now shows as **locked** (the star protection became the lock; nothing you meant to keep is exposed). — `⚠️ needs real save` (migration is unit-tested; needs a real favorited save to see live)
- [ ] Export the save, re-import it: loads clean (save format 53). — `⚠️ needs real save`
- [ ] No console errors on load or while navigating the tabs. — `✅ auto` (no console errors across the whole session, desktop + mobile)

## 1. Craft -> Inspect -> Roll (item lifecycle)
_`⚠️ needs real save` for the whole section: reaching a working Fabricator from a fresh sandbox means building the Foundry up from scratch (many steps). The blanks/inspect engine is unit-tested (inspectBlank.test.ts); verify the live loop on your migrated save._
- [ ] Fabricate a system. It arrives as a **blank** (stackable), NOT a finished rolled item. Two of the same recipe **stack**.
- [ ] Open the Fabricator **Inspect** tab. Inspecting a blank **rolls** it into a system with quality/rarity/stats; it lands in the spare bay (Logistics > Ship Equipment).
- [ ] The `?` on **Inspect Blanks** explains blanks; the panel is not cluttered with the old green paragraph.
- [ ] Regression: crafting XP / crafting level still advance from crafting; the craft queue still runs and reserves materials.

## 2. The Armory (loadouts)
- [ ] Create a loadout for a hull type; rename it; delete a different one.
- [ ] Add rolled systems to the loadout; the slot derivation + Battle Rating read correctly.
- [ ] **Check out** the loadout to a ship: the ship equips the whole set in one step.
- [ ] The checked-out ship's own install screen is **locked** (edit happens in the Armory instead).
- [ ] **Check in** frees the ship again.
- [ ] The `?` on **Armory - Loadouts** carries the intro; no green paragraph below the title.
- [ ] Regression (bug fix): try to **salvage / scrap / uninstall** gear that is committed to a loadout, it is **protected** (cannot be lost).

## 3. The Archive
- [ ] Enshrine opens a **picker** (2026-09-18 change): the row button reads "Enshrine…" and opens a modal to choose the exact spare; it does NOT one-tap consume the best. — `✅ auto`
- [ ] You must **pick** a spare: the Enshrine button is disabled until one is selected. — `✅ auto`
- [ ] Enshrine a spare: its score is recorded, it is **consumed**, and the **completion % / record** updates. — `✅ auto` (record 0 → 12.0K, count 2 → 1)
- [ ] **No-gain warning before submit**: selecting a spare that can't beat the record shows an amber "will not beat your record … consumes it for no gain" note and the button becomes **"Enshrine anyway"**. — `✅ auto`
- [ ] A **better roll** raises the record; an equal/worse one does not (max is kept). — `✅ auto`
- [ ] The `?` on **The Archive** carries the intro.

## 4. Fuel becomes Reach
- [ ] Header has **no fuel readout**. Facilities has **no Fuel Depot card**. Operations has **no Local Deuterium Skim** mission.
- [ ] FIX (2026-09-18): the ship-detail Exploration/Prospecting readout shows a single **Range (ly)** row, not the old "Fuel Efficiency ×0.00" / "Fuel Capacity" rows (same in the install compare pane). — `✅ auto` (shows "Range 100 ly")
- [ ] FIX (2026-09-18): the weapon tooltip's range stat reads **"Weapons Range"** (disambiguated from the hull's travel Range). — static label swap; confirm on a weapon tooltip live.
- [ ] Dispatch a mission and a patrol: the cards / dispatch windows show a trip **Distance** and the hull's **Range** in **lightyears** (not a fuel cost).
- [ ] A mission beyond a hull's range is **flagged / blocked** before dispatch; an in-range one dispatches normally.
- [ ] Refuel is effectively free/instant: a captain never stalls "out of fuel", and no captain is tied to fuel duty.
- [ ] Regression: missions still pay out, cargo still unloads, patrols still fight, offline catch-up still resolves.

## 5. Quartermaster - Sell
_`⚠️ needs real save` for the whole section: a fresh post-fuel-rework save generates no Deuterium Ice, so the Sell shelf can't be populated in the sandbox. Test on your migrated save, which carries the ice the fuel change left behind._
- [ ] With Deuterium Ice on hand, open **Quartermaster > Sell**: it lists Deuterium Ice with a **Retired** tag, owned count, and **40 cr** unit value.
- [ ] The **- / +** stepper and the editable box change the quantity; **Sell** credits you `qty x 40` and reduces the stock; wallet updates.
- [ ] Sell all of it: the shelf empties to the placeholder ("nothing to sell").
- [ ] **[M]** The **Retired** tag shows in full (not clipped to "Retire...").
- [ ] Regression: nothing that lacks a sell value ever appears here or can be sold.

## 6. Favorite and Lock (spare bays)
Two surfaces show the toggles: **Logistics > Ship Equipment** and **Facilities > Salvage Bay**. Both share the selection and both host the controls.
- [ ] In **Ship Equipment**, tap a spare: an **inline** card opens below the grid (not a floating popover) with **Favorite** and **Lock** buttons. — `✅ auto` (desktop + mobile)
- [ ] **Favorite** a spare: it gains a **star** marker and sorts to the **top of its group**; the button reads "★ Favorited". Un-favoriting drops it back. — `✅ auto` (favorited a bottom spare, it jumped to position 1)
- [ ] **Lock** a spare: it gains a **padlock** marker; the button reads "🔒 Locked". A piece can show **both** the star and the padlock. — `✅ auto` (both markers coexisted on one tile)
- [ ] In the **Salvage Bay**, select the locked spare: the **Salvage / Destroy** button is **disabled**, with a "Locked, so nothing can salvage it. Unlock it to salvage." note. — `✅ auto`
- [ ] **Unlock** it (from either surface): Salvage / Destroy re-enables immediately. — `✅ auto`
- [ ] Try to **install** a locked spare on a ship (Ships > a ship > Install/Swap): it is **blocked** with a "locked (unlock it in Ship Equipment first)" reason. — `✅ auto` (shows "Blocked: locked (unlock it in Ship Equipment first)")
- [ ] Try to **commit** a locked spare to an Armory loadout: it is **refused**, and the picker shows it **disabled with a lock reason**. — `✅ auto` locked candidate now renders disabled + dimmed + 🔒 with a "Locked, so it cannot be committed..." title (finding #1 fixed 2026-09-18); unlocked twin still commits.
- [ ] Regression: a merely **favorited** (not locked) spare can still be salvaged, installed, and committed normally, favorite is display-order only. — `✅ auto` (install + commit of the unlocked twin both worked)
- [ ] Regression: with auto-salvage rules ON, a **locked** spare is never queued; an unlocked one is subject to the rules as before ("Lock it to keep it." note). — unit-tested (parity); not driven live this pass
- [ ] **[M]** The Favorite / Lock buttons and their note wrap cleanly; the tile markers are not clipped. — `✅ auto` (375px)

## 7. Currencies band
- [ ] Header shows the currencies as a soft-glowing **band** with pills split evenly (Credits + Admin Points today).
- [ ] Tap / hover a pill: its **info tooltip** appears (name + description).
- [ ] **[D]** Band is full-width below the header row; name tags visible.
- [ ] **[M]** Band is compact (glyph + value); tapping a pill shows the tooltip.
- [ ] **Settings > Visual > Header Currencies**: toggling a currency updates the band live; turning **both off** shows the "pick currencies in Options" placeholder.
- [ ] Values still update every tick (credits after a mission payout, admin points on rank up).

## 8. Action windows (modals)
- [ ] Install a system, a confirmation prompt, a ship/system picker, and the while-you-were-away summary each open the **same shell**.
- [ ] **[M]** Bottom sheet, reachable, closes on backdrop tap / X / Escape.
- [ ] **[D]** Centered card; focus is trapped; Escape closes.

## 9. Presentation polish
- [ ] Ship detail: **back arrow + class name + Assign/Salvage** on **one row** [D]; wraps gracefully [M]. — `✅ auto` (one row on desktop; mobile wrap not re-checked)
- [ ] Ship picker options show **BR / Holds / Range** + a **favorite star**.
- [ ] Facility `?` tooltips (Salvage Bay, Shipyard, Armory, Archive, Docks, Inspect Blanks, Quartermaster): text reads correctly, panels look clean.
- [ ] Docks still shows the "hull management now lives in Ships" pointer (kept visible on purpose).
- [ ] Logistics > Materials: spacing above the first panel matches the other tabs.
- [ ] Ops Overview / Gathering / Combat section headers read like the Home dashboard (label + count + rule).
- [ ] Header **portrait** opens Crew > Admiral (Profile); **gear** opens Settings.
- [ ] FIX (2026-09-18): a locked "Coming soon" sub-tab shows its **tooltip on hover** (was a disabled button that ate the hover). — code-verified; please confirm live.
- [ ] FIX (2026-09-18): a warehouse tile reads its **true %** and only shows 100% when actually capped (was `Math.round` bumping 99.6% to a non-red "100%"). — code-verified; needs a near-full item to see live.

## 10. Themes (incl. the four new)
- [ ] Settings > Visual > Theme: switch through all themes including **Purple**, **Pink**, **Chartreuse**, and **Vermilion**.
- [ ] On each, check text contrast is readable AND the **currencies band glow** re-hues with the theme (no stuck cyan on another accent).
- [ ] The Sell tab, the band, and the `?` tooltips all look right per theme.

## 11. Cross-cutting regression
- [ ] Save persists across a reload on both platforms (no "not saving" banner unless genuinely blocked).
- [ ] Reduce-motion honored on progress bars / animations.
- [ ] No horizontal scroll at ~375px anywhere touched (band, Sell rows, ship header, modals).
- [ ] Interface scaling (0.9x - 1.5x) still lays the header/band out sanely.

---

### Notes / found issues
(record anything here as you go)

**Found in the automated pass (Claude, 2026-09-18):**
1. **✅ FIXED 2026-09-18 — Armory pool picker feedback on a LOCKED spare.** Previously a locked spare showed as a normal candidate chip and clicking it silently did nothing (engine refused correctly, no UI feedback). Now the locked candidate renders disabled + dimmed with a 🔒 and the title "Locked, so it cannot be committed. Unlock it in Ship Equipment first," matching the ship install picker. Verified in the preview.
2. **✅ FIXED 2026-09-18 — vestigial fuel stats.** The ship-detail readout + install compare pane replaced "Fuel Capacity" and "Fuel Efficiency ×0.00" with a single honest **Range** row in lightyears (derived like the dispatch/roster range); the weapon tooltip's range stat became "Weapons Range" to disambiguate. The deep model removal of the underlying fuel fields still rides the content patch; this stops the panel showing retired fuel vocabulary.
