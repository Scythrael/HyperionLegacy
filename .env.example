# DEV_MODE gates the debug panel (speed control, resource grants, offline
# simulator). Never true in a production build. Vercel: set VITE_DEV_MODE=true
# for the Preview environment only, leave unset/false for Production.
VITE_DEV_MODE=true
